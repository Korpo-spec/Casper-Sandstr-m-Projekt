class Tower {
    constructor(position, damage, fireRate, range){
        this.position = position;
        this.damage = damage;
        this.fireRate = fireRate;
        this.range = range;

        let rotation = 0;
    }
}